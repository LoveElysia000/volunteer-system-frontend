export const WORKBENCH_BREAKPOINTS = {
  mobileMax: 1023,
  compactMax: 1279
} as const

export const VOLUNTEER_SIDEBAR = {
  storageKey: 'volunteer_center_sidebar_width',
  defaultWidth: 296,
  minWidth: 256,
  maxWidth: 420,
  compactWidth: 92,
  mobileDrawerWidth: 302,
  keyboardStep: 16
} as const

export const ORGANIZATION_SIDEBAR = {
  storageKey: 'organization_center_sidebar_width',
  defaultWidth: 304,
  minWidth: 256,
  maxWidth: 420,
  mobileDrawerWidth: 304,
  keyboardStep: 16
} as const
