apiVersion: v1
kind: Service
metadata:
  name: volunteer-system-frontend
  namespace: volunteer-system
  labels:
    app: volunteer-system-frontend
spec:
  selector:
    app: volunteer-system-frontend
  ports:
    - port: 80
      targetPort: 8080
